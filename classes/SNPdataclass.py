class SNPclass():
    def __init__(self, data, rsid, chr, loc, pheno, fid, pid, gender):
        self.data = data
        self.rsid = rsid
        self.chr = chr
        self.loc = loc
        self.pheno = pheno
        self.fid = fid
        self.pid = pid
        self.gender = gender
